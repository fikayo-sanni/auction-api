version: '3.8'

services:
  app:
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - '3000:3000'
    environment:
      - NODE_ENV=production
      - DATABASE_URL=
      - INFURA_PROJECT_ID=
      - ACCOUNT_PRIVATE_KEY=
      - CONTRACT_ADDRESS=
      - NODE_PROVIDER_URL=
